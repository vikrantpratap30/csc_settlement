package com.csc.settlement.repository;

import com.csc.settlement.entity.RefundPart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefundPartRepository extends JpaRepository<RefundPart, Long> {
}
