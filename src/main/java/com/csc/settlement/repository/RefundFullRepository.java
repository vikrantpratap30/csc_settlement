package com.csc.settlement.repository;

import com.csc.settlement.entity.RefundFull;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefundFullRepository extends JpaRepository<RefundFull, Long> {
}
