package com.csc.settlement.repository;

import com.csc.settlement.entity.ProductPaysheet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductPaysheetRepository extends JpaRepository<ProductPaysheet, Long> {
}
